# LLM API 配置（仅在无法读取 OpenCode 配置时使用）
# 主方案：Pipeline 默认读取 ~/.config/opencode/opencode.jsonc，无需此文件
# 本文件仅在以下场景使用：
#   - 不使用 OpenCode，直接命令行运行
#   - CI/CD 环境
#   - opencode.jsonc 格式不兼容
#   - 需要覆盖 OpenCode 的模型选择
#
# 支持 DeepSeek、百炼、OpenAI 等兼容 OpenAI API 格式的提供商
LLM_API_KEY=sk-your-api-key-here
LLM_BASE_URL=https://api.deepseek.com
LLM_MODEL=deepseek-v4-pro

# 项目根目录 (可选, 默认自动推导)
# 仅在路径推导失败或项目位置非常规时需要手动设置
# STOCK_ANALYSIS_HOME=/path/to/stock-analysis